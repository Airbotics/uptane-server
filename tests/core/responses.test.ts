import { SuccessMessageResponse } from '@airbotics-core/network/responses';


test('should return correct key storage key id for a repo', async () => {

    // expect(new SuccessMessageResponse(res, 'next')).toEqual();
});


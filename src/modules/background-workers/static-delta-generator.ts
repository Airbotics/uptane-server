import config from '@airbotics-config';
import { logger } from '@airbotics-core/logger';


/**
 * Generate static deltas between ostree commits.
 */
export const generateStaticDeltas = async () => {

    logger.info('running background worker to generate static deltas');

    // TODO implement

    logger.info('completed background worker to generate static deltas');

}